# Offline evaluation set

The evaluation set checks whether each public pack returns the exact bindings
declared in its `expected/` fixture. Run a case with:

```bash
ontokit eval examples/hello-ontology manager
```

These are regression fixtures, not a benchmark of an LLM, a client's data, or a
clinical or commercial outcome. Extend them with expert-reviewed counterexamples
when a domain pack grows.

