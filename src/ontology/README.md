# Sample Ontology Assets

This directory contains a minimal RDF data graph and SHACL shapes for the repository's validation workflow.

- `data.ttl`: sample instance data
- `shapes.ttl`: constraints for validating the sample graph
