# Sample Ontology Assets

This directory contains a minimal RDF data graph and SHACL shapes so the repository's ontology validation workflow is practical and runnable.

- `data.ttl`: sample instance data
- `shapes.ttl`: constraints for validating the sample graph
