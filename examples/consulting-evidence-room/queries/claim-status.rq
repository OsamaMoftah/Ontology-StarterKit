PREFIX ex: <https://ontology-starterkit.dev/consulting/>
SELECT ?claim ?text ?evidenceStatus WHERE {
  ?claim a ex:Claim ; ex:text ?text .
  BIND(IF(EXISTS { ?claim ex:supportedBy ?evidence }, "has-evidence", "no-evidence") AS ?evidenceStatus)
}
